# Server-only secret used by app/api/chat/route.ts
GROQ_API_KEY=

# Public contact address used to open the visitor's email application
NEXT_PUBLIC_CONTACT_EMAIL=

# Optional public URL to a CV (for example /cv.pdf or an HTTPS URL)
NEXT_PUBLIC_CV_URL=
